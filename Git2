N = int(input('Bekat: '))
a = int(input('Enter: '))
b = int(input('Exit: '))
M = int(input('Jami: '))

person = 0
for i in range(N - 1):
    people = int(input('People: '))
    person += people
    person -= a
    if person < 0:
        person = 0
    person += b

person += M
print(person)
